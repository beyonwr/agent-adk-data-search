# =============================================================================
# Production Environment Configuration
# =============================================================================
# Copy this file and rename to .env.production for production deployment
# Update all values with your actual production credentials

# --- LLM Configuration ---
# Replace with your actual LLM API credentials
PADO_API_KEY=your_production_pado_api_key
PADO_MODEL_NAME=your_production_model_name
PADO_MODEL_API=https://your-production-llm-api.com/v1

# OpenAI-compatible API settings
GPT_OSS_120B_API_BASE=${PADO_MODEL_API}
GPT_OSS_120B_MODEL=${PADO_MODEL_NAME}
OPENAI_API_KEY=${PADO_API_KEY}

# --- Agent Configuration ---
ROOT_AGENT_API_BASE=${GPT_OSS_120B_API_BASE}
ROOT_AGENT_MODEL=${GPT_OSS_120B_MODEL}

SUB_AGENT_API_BASE=${GPT_OSS_120B_API_BASE}
SUB_AGENT_MODEL=${GPT_OSS_120B_MODEL}

DATA_SEARCH_AGENT_BASE=${GPT_OSS_120B_API_BASE}
DATA_SEARCH_MODEL=${GPT_OSS_120B_MODEL}

# --- Embedding Model Configuration ---
# If using external embedding server, update with your production URL
# If using docker-compose embedding service, use: http://embedding:18000/v1/embeddings
TEXT_EMBEDDING_MODEL_URL=http://your-production-embedding-host:18000/v1/embeddings
TEXT_EMBEDDING_MODEL_NAME=bge-m3-ko

# --- PostgreSQL Database Configuration ---
# Docker Compose will automatically use 'postgres' as hostname
# For external database, update POSTGRESQL_DB_HOST
POSTGRESQL_DB_USER=postgres
POSTGRESQL_DB_PASS=your_secure_postgres_password_here
POSTGRESQL_DB_NAME=agent_production_db
POSTGRESQL_DB_HOST=postgres
POSTGRESQL_DB_PORT=5432
POSTGRESQL_DB_TABLE=your_production_table_name

# --- ChromaDB Configuration ---
# Docker Compose will automatically use 'chromadb' as hostname
# For external ChromaDB, update CHROMADB_HOST
CHROMADB_HOST=chromadb
CHROMADB_PORT=8000
CHROMADB_COLLECTION_NAME=your_production_collection_name

# --- Application Configuration ---
# Python environment
PYTHONUNBUFFERED=1
PYTHONPATH=/app

# Logging
LOG_LEVEL=INFO
LOG_FILE=/app/logs/app.log

# --- Security Notes ---
# 1. NEVER commit this file with real credentials to version control
# 2. Use strong, unique passwords for production
# 3. Consider using secret management services (AWS Secrets Manager, HashiCorp Vault, etc.)
# 4. Rotate credentials regularly
# 5. Use environment-specific API keys (dev, staging, prod)
